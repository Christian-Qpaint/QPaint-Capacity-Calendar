// Placeholder until the schema is live in Supabase and types are regenerated with:
//   npx supabase gen types typescript --project-id ukrvxyyhaplwngsrosyn > src/types/database.ts
// Using `any` here (not the DB generic) rather than fighting a type mismatch before real types exist.
export type Database = any
