// Shared assumption used to invent internally-consistent placeholder $ figures where the locked
// spec doesn't define a rate (e.g. converting a QPaint Team's hour-capacity into a $ figure for the
// Capacity Board's summary tiles). Not one of the 10 locked formulas — flagged as an assumption.
export const STANDARD_DOLLAR_RATE_PER_HOUR = 137
